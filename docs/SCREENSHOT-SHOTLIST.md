# Screenshot Shot List

Use this only if you want to refresh the screenshots later. The repo already includes the captured PNG files under `docs/screenshots/`.

Use a desktop browser width around 1440px. Keep the app on the guided view, use the bundled synthetic sample dataset, and avoid showing local file-system chrome where possible.

## 01-home-or-upload.png

- Step 1 upload view with `Deterministic` selected
- Keep the upload form visible with the bundled demo-file note
- Keep the overall chrome clean and product-led

## 02-profile-or-schema-view.png

- Step 2 pre-check summary after uploading `data/sample_messy.csv`
- Include readiness, decision, primary domain, schema snapshot, and column signals
- Prefer the deterministic path so the screenshot stays focused on the core product

## 03-cleanup-or-validation-results.png

- Step 4 result view after cleanup finishes
- Keep the quality summary and download action visible
- Avoid noisy warning or validation-note clutter in the main README screenshot

## 04-export-or-quality-summary.png

- History & Diagnostics view with a selected dataset and run
- Keep run engine metadata visible
- Keep the diagnostics panel visible enough to prove traceability without overwhelming the frame
