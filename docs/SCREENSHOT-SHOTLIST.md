# Screenshot Shot List

Use this only if you want to refresh the screenshots later. The repo already includes the captured PNG files under `docs/screenshots/`.

Use a desktop browser width around 1440px. Keep the app on the guided view, use the bundled synthetic sample dataset, and avoid showing local file-system chrome where possible.

## 01-home-or-upload.png

- Step 1 upload view
- Show the deterministic and Ollama mode cards
- Keep the upload form visible with the bundled demo-file note
- Show healthy API and workflow badges

## 02-profile-or-schema-view.png

- Step 2 pre-check summary after uploading `data/sample_messy.csv`
- Include readiness, decision, primary domain, blockers, and recommended actions
- If Ollama mode is active, include planner summary and plan pills

## 03-cleanup-or-validation-results.png

- Step 3 cleanup view after moving past pre-check
- Open the optional run customization area
- Keep current readiness, cleanup settings, and the primary action visible

## 04-export-or-quality-summary.png

- Step 4 result view after guided cleanup finishes
- Keep the quality decision, recommendation, and download action visible
- Avoid extra browser chrome so the summary reads cleanly in the README
