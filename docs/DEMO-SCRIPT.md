# 60-Second Demo Script

1. "This is a local-first healthcare data cleanup workflow for messy claims and EHR-style exports."
2. "I start with the bundled synthetic sample so the entire demo is public-safe."
3. "The first step lets me choose deterministic cleanup or an optional local Ollama model that helps interpret the schema."
4. "After upload, the app profiles the dataset, flags readiness blockers, and shows recommended actions."
5. "Then I run guided cleanup. The actual transforms stay deterministic, so the result is auditable even when the planner is AI-assisted."
6. "On the result screen, I get a quality summary, warnings, and a direct export path."
7. "If I want traceability, I can open History and Diagnostics to inspect the engine, model, and run details."

Close with: "The value is not just cleaning a CSV. It is making healthcare data cleanup faster, more understandable, and safer to hand off into analytics or AI workflows."
